platform: template
sensors:
  livingroom_occupied:
    device_class: occupancy
    friendly_name: 'Livingroom occupied'
    entity_id:
      - media_player.android_tv
      - media_player.living_room
    value_template: >-
      {{ 
          (
            is_state('input_boolean.automatic_livingroom_occupancy', 'off') and is_state('input_boolean.override_livingroom_occupancy')
          )
            or
          (
            (
              not is_state('media_player.android_tv', 'unavailable')
              and
              not is_sate('media_player.android_tv', 'off')
            )
            or is_state('media_player.living_room', 'playing')
          )
      }}
  